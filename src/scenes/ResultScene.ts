// src/scenes/ResultScene.ts
import Phaser from "phaser";

export class ResultScene extends Phaser.Scene {
    constructor() { super({ key: "ResultScene" }); }

    create(data: {stage: number, score: number}) {

    }
}
